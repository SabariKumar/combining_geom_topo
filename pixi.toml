[workspace]
authors = ["Sabari Kumar <sabarinkumar@gmail.com>"]
channels = ["dglteam/label/th24_cu124","conda-forge", "https://prefix.dev/conda-forge", "pytorch"]
name = "combining_geom_topo"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]

[dependencies]
python = ">=3.10"
pytorch = ">=2.4"
pytorch-cuda = { version = "12.4.*", channel = "pytorch" }
dgl = ">=2.0"
numpy = ">=1.24,<2"
pandas = ">=2.0"
networkx = ">=3.0"
click = ">=8.0"
tqdm = ">=4.60"
biopython = ">=1.80"
wandb = ">=0.15"
einops = ">=0.7"
scikit-learn = ">=1.3"
loguru = ">=0.7"
matplotlib = ">=3.7"
pynvml = ">=11.5"

[pypi-dependencies]
e3nn = ">=0.5"
graphein = ">=1.7"
fair-esm = ">=2.0"

[activation.env]