[workspace]
authors = ["Sabari Kumar <sabarinkumar@gmail.com>"]
channels = ["pytorch", "dglteam/label/th24_cu124", "nvidia", "conda-forge"]
name = "combining_geom_topo"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]

[dependencies]
python = ">=3.10"
pytorch = { version = ">=2.4,<2.5", channel = "pytorch" }
pytorch-cuda = { version = "12.4.*", channel = "pytorch" }
dgl = ">=2.0"
numpy = ">=1.24,<2"
pandas = ">=2.0"
networkx = ">=3.0"
click = ">=8.0"
tqdm = ">=4.60"
biopython = ">=1.80"
wandb = ">=0.15"
einops = ">=0.7"
scikit-learn = ">=1.3"
loguru = ">=0.7"
matplotlib = ">=3.7"
pynvml = ">=11.5"
gudhi = ">=3.8"

[pypi-dependencies]
e3nn = ">=0.5"
graphein = ">=1.7"
fair-esm = ">=2.0"

[activation.env]